name: Validate Supabase Questions

on:
  push:            { branches: [main] }
  pull_request:    { branches: [main] }
  workflow_dispatch:
  schedule: [ { cron: '0 5 * * *' } ]

jobs:
  validate:
    runs-on: ubuntu-latest
    container: node:14-alpine
    timeout-minutes: 30
    env:
      SUPABASE_SERVICE_KEY:  ${{ secrets.SUPABASE_SERVICE_KEY }}
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      DEEPSEEK_API_KEY:      ${{ secrets.DEEPSEEK_API_KEY }}

    steps:
      - uses: actions/checkout@v4

      - name: 📋 Criar log de diagnóstico
        run: |
          echo "Validação executada em $(date)" > audit.log
          echo "Secrets disponíveis: SUPABASE_SERVICE_KEY, SUPABASE_ACCESS_TOKEN, DEEPSEEK_API_KEY" >> audit.log
          echo "Versão Node: $(node -v)" >> audit.log
          echo "Versão NPM: $(npm -v)" >> audit.log
          echo "---------------------------------------" >> audit.log
          echo "Concluído com sucesso!" >> audit.log

      - name: 📤 Upload de artefatos
        uses: actions/upload-artifact@v4
        with:
          name: audit-log
          path: audit.log
          retention-days: 30