{
  "name": "supabase-question-auditor",
  "version": "1.0.0",
  "description": "Auditor de questões utilizando Supabase e OpenAI para a plataforma Algebraticamente.",
  "main": "dist/validateQuestions.js", // <-- AJUSTADO para o novo arquivo principal compilado
  "type": "module",
  "scripts": {
    "start": "node dist/validateQuestions.js", // <-- AJUSTADO para rodar o compilado
    "dev": "nodemon --watch './*.ts' --exec 'ts-node' validateQuestions.ts", // <-- AJUSTADO para observar a raiz e rodar o TS principal
    "build": "tsc", // <-- OK (usa tsconfig.json)
    "lint": "eslint . --ext .ts", // <-- OK
    "test": "echo \"Error: no test specified\" && exit 1", // <-- OK
    "audit:questions": "node dist/validateQuestions.js" // <-- AJUSTADO para rodar o compilado (usado pelo GitHub Actions)
  },
  "keywords": [
    "supabase",
    "openai",
    "auditor",
    "questions",
    "education",
    "typescript",
    "deepseek"
  ],
  "author": "roneymatusp2 <seuemail@example.com>", // <-- OK
  "license": "ISC", // <-- OK
  "dependencies": { // <-- OK
    "@supabase/supabase-js": "^2.49.4",
    "dotenv": "^16.5.0",
    "openai": "^4.97.0"
  },
  "devDependencies": { // <-- OK
    "@types/node": "^20.17.43",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.1.3",
    "nodemon": "^3.0.3",
    "prettier": "^3.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "engines": { // <-- OK
    "node": ">=18.0.0"
  }
}
